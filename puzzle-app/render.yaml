services:
  - type: web
    name: puzzle-app
    env: node
    rootDirectory: puzzle-app
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 10000
